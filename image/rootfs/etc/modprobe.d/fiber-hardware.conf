# Fiber hardware policy.
#
# Keep driver loading automatic through udev/modalias matching. Options here are
# conservative defaults that improve compatibility without forcing a GPU, Wi-Fi,
# Bluetooth, or sound driver on hardware that does not need it.

options snd_hda_intel power_save=1
options btusb enable_autosuspend=1
